// Re-export the AI-related types from the engine root so consumers can
// import everything AI-related from one place.
export type {
  FactionAgent,
  AgentContext,
  StrategicCommand,
  TacticalCommand,
  Personality,
} from '../types.js';
